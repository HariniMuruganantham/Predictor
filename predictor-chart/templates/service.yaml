apiVersion: v1
kind: Service
metadata:
  name: {{ include "predictor-chart.fullname" . }}
  labels:
    {{- include "predictor-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "predictor-chart.selectorLabels" . | nindent 4 }}
